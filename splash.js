document.addEventListener('DOMContentLoaded', function () {
    // Esperar 3 segundos y luego redirigir
    setTimeout(() => {
        window.location.replace('index.html'); // Cambiar a la página principal
    }, 3000); // Tiempo en milisegundos
});
